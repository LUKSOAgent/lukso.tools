import { LSPFactoryOptions } from '../interfaces';
import { LSP4MetadataBeforeUpload, LSP4MetadataContentBeforeUpload, LSP4MetadataUrlForEncoding } from '../interfaces/lsp4-digital-asset';
import { UploadOptions } from '../interfaces/profile-upload-options';
export declare class LSP4DigitalAssetMetadata {
    options: LSPFactoryOptions;
    constructor(options: LSPFactoryOptions);
    static uploadMetadata(metaData: LSP4MetadataContentBeforeUpload | LSP4MetadataBeforeUpload, uploadOptions?: UploadOptions): Promise<LSP4MetadataUrlForEncoding>;
    uploadMetadata(metaData: LSP4MetadataContentBeforeUpload | LSP4MetadataBeforeUpload, uploadOptions?: UploadOptions): Promise<LSP4MetadataUrlForEncoding>;
}
