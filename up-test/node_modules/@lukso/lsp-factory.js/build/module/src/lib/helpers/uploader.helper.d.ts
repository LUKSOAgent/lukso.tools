import { AddResult } from 'ipfs-core-types/src/root';
import { ImportCandidate } from 'ipfs-core-types/src/utils';
import { ImageBuffer, ImageMetadata } from '../interfaces';
import { AssetBuffer, AssetMetadata } from '../interfaces/metadata';
import { IPFSGateway, UploadOptions } from '../interfaces/profile-upload-options';
export declare const defaultSizes: number[];
export declare function imageUpload(givenFile: File | ImageBuffer, uploadOptions: UploadOptions, sizes?: number[]): Promise<ImageMetadata[]>;
export declare function assetUpload(asset: File | AssetBuffer, uploadOptions: UploadOptions): Promise<AssetMetadata>;
export declare function ipfsUpload(file: ImportCandidate, ipfsGateway: IPFSGateway): Promise<AddResult>;
export declare function prepareMetadataImage(uploadOptions?: UploadOptions, image?: File | ImageBuffer | ImageMetadata[], sizes?: number[]): Promise<ImageMetadata[] | null>;
export declare function prepareMetadataAsset(asset: File | AssetBuffer | AssetMetadata, uploadOptions?: UploadOptions): Promise<AssetMetadata>;
export declare function isMetadataEncoded(metdata: string): boolean;
export declare function formatIPFSUrl(ipfsGateway: IPFSGateway, ipfsHash: string): string;
