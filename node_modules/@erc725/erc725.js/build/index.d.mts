import { E as ERC725JSONSchema, D as DynamicKeyParts, a as DynamicKeyPart, b as ERC725JSONSchemaValueType } from './ERC725JSONSchema-CWuvjkUi.mjs';
export { e as DynamicNameSchema, c as ERC725JSONSchemaKeyType, d as ERC725JSONSchemaValueContent } from './ERC725JSONSchema-CWuvjkUi.mjs';
import { Hex } from 'viem';

interface Permissions {
    CHANGEOWNER?: boolean;
    ADDCONTROLLER?: boolean;
    EDITPERMISSIONS?: boolean;
    ADDEXTENSIONS?: boolean;
    CHANGEEXTENSIONS?: boolean;
    ADDUNIVERSALRECEIVERDELEGATE?: boolean;
    CHANGEUNIVERSALRECEIVERDELEGATE?: boolean;
    REENTRANCY?: boolean;
    SUPER_TRANSFERVALUE?: boolean;
    TRANSFERVALUE?: boolean;
    SUPER_CALL?: boolean;
    CALL?: boolean;
    SUPER_STATICCALL?: boolean;
    STATICCALL?: boolean;
    SUPER_DELEGATECALL?: boolean;
    DELEGATECALL?: boolean;
    DEPLOY?: boolean;
    SUPER_SETDATA?: boolean;
    SETDATA?: boolean;
    ENCRYPT?: boolean;
    DECRYPT?: boolean;
    SIGN?: boolean;
    EXECUTE_RELAY_CALL?: boolean;
    ERC4337_PERMISSION?: boolean;
    ALL_PERMISSIONS?: boolean;
    [key: string]: boolean | undefined;
}

declare enum SUPPORTED_VERIFICATION_METHOD_STRINGS {
    KECCAK256_UTF8 = "keccak256(utf8)",
    KECCAK256_BYTES = "keccak256(bytes)"
}
declare enum SUPPORTED_VERIFICATION_METHOD_HASHES {
    HASH_KECCAK256_UTF8 = "0x6f357c6a",
    HASH_KECCAK256_BYTES = "0x8019f9b1"
}
type SUPPORTED_VERIFICATION_METHODS = SUPPORTED_VERIFICATION_METHOD_STRINGS | SUPPORTED_VERIFICATION_METHOD_HASHES;

interface KeyValuePair {
    key: string;
    value: any;
}
interface URLData {
    url: string;
}
interface Verification {
    data: string;
    method: SUPPORTED_VERIFICATION_METHODS | string;
    source?: string;
}
interface URLDataWithHash extends URLData {
    verification: Verification;
    json?: never;
}
interface URLDataWithJson extends URLData {
    verification?: Verification;
    json: Record<string, any>;
}
type URLDataToEncode = URLDataWithHash | URLDataWithJson;
type EncodeDataType = string | string[] | URLDataToEncode | boolean | number | bigint | (string | number | bigint)[];
interface EncodeDataReturn {
    keys: string[];
    values: string[];
}

interface ERC725Config {
    /**
     * ```js title=Example
     * const config = {
     *   ipfsGateway: 'https://ipfs.lukso.network/ipfs/'
     *   gas: 20_000_000 // Optional, default 0
     * };
     * ```
     * Make sure to use the following format: `<url>/ipfs/`.<br/>
     * Another example: `https://cloudflare-ipfs.com/ipfs/`
     */
    ipfsGateway?: string;
    ipfsFetch?: (url: string, init?: unknown) => Promise<any>;
    ipfsConvertUrl?: (url: string) => string;
    gas?: number;
    throwSchemaErrors?: boolean;
}
interface ERC725Options {
    schemas: ERC725JSONSchema[];
    address?: string;
    provider?: any;
    ipfsGateway: string;
    ipfsFetch?: (url: string, init?: unknown) => Promise<any>;
    ipfsConvertUrl?: (url: string) => string;
    gas: number;
}

interface DataInput {
    keyName: string;
    value: unknown;
    dynamicKeyParts?: string | string[] | number | bigint;
    totalArrayLength?: number;
    startingIndex?: number;
}
interface EncodeDataInput extends DataInput {
    value: EncodeDataType;
}
interface DecodeDataInput extends DataInput {
    value: string | {
        key: string;
        value: string | null;
    }[];
}
type Data = string | bigint | boolean | null;
interface DecodeDataOutput {
    value: Data | Data[] | URLDataWithHash | null;
    name: string;
    key: string;
    dynamicName?: string;
}
interface FetchDataOutput {
    value: null | string | string[] | {
        LSP3Profile: Record<string, any>;
    } | Record<string, any>;
    error?: Error;
    dynamicKeyParts?: DynamicKeyParts;
    dynamicName?: string;
    name: string;
    key: string;
}
interface GetDataExternalSourcesOutput extends DecodeDataOutput {
    value: any;
}

interface GetDataDynamicKey {
    keyName: string;
    dynamicKeyParts: string | string[];
}
type GetDataInput = string | GetDataDynamicKey | Array<string | GetDataDynamicKey>;

interface AssetURLEncode {
    verification: Verification;
    url: string;
}

declare enum ProviderTypes {
    ETHEREUM = "ETHEREUM",
    WEB3 = "WEB3"
}

type ConsumedPtr = {
    bytes: number;
};

/**
 * @file lib/utils.ts
 * @author Robert McLeod <@robertdavid010>
 * @author Fabian Vogelsteller <fabian@lukso.network>
 * @author Hugo Masclet <@Hugoo>
 * @date 2020
 */

/**
 *
 * @param key The schema key of a schema with keyType = 'Array'
 * @param index An integer representing the intended array index
 * @return The raw bytes key for the array element
 */
declare function encodeArrayKey(key: string, index: number): `0x${string}`;
/**
 * @param schema an array of schema definitions as per ${@link ERC725JSONSchema}
 * @param data an object of key-value pairs
 */
declare function encodeData(data: EncodeDataInput | EncodeDataInput[], schema: ERC725JSONSchema[]): EncodeDataReturn;
declare function getVerificationMethod(nameOrSig: string): {
    method: (data: string | object | Uint8Array | null) => string;
    name: SUPPORTED_VERIFICATION_METHOD_STRINGS;
    sig: SUPPORTED_VERIFICATION_METHODS;
} | undefined;
/**
 * Hashes the data received with the specified hashing function,
 * and compares the result with the provided hash.
 */
declare function isDataAuthentic(data: string | Uint8Array, options: Verification, capture?: string[]): boolean;
/**
 * Changes the protocol from `ipfs://` to `http(s)://` and adds the selected IPFS gateway.
 * `ipfs://QmbKvCVEePiDKxuouyty9bMsWBAxZDGr2jhxd4pLGLx95D => https://ipfs.lukso.network/ipfs/QmbKvCVEePiDKxuouyty9bMsWBAxZDGr2jhxd4pLGLx95D`
 */
declare function patchIPFSUrlsIfApplicable(receivedData: URLDataWithHash, ipfsGateway: string | ERC725Options): URLDataWithHash;

/**
 * @param schema schema is an array of objects of schema definitions
 * @param data data is an array of objects of key-value pairs
 *
 * @return: all decoded data as per required by the schema and provided data
 */
declare function decodeData(data: DecodeDataInput[], schema: ERC725JSONSchema[], throwExceptions?: boolean): DecodeDataOutput[];
declare function decodeData(data: DecodeDataInput, schema: ERC725JSONSchema[], throwExceptions?: boolean): DecodeDataOutput;

/**
 * @file lib/encodeKeyName.ts
 * @author Hugo Masclet <@Hugoo>
 * @date 2022
 */

declare function isDynamicKeyName(name: string): boolean;
/**
 *
 * @param name the schema element name.
 * @param dynamicKeyParts
 *
 * @return the name of the key encoded as per specifications.
 */
declare function encodeKeyName(name: string, dynamicKeyParts?: DynamicKeyParts): string;

/**
 * @param keyHash hashed key with the dynamic parts
 * @param keyNameOrSchema key name of schema definitions or schema
 *
 * @return: all decoded dynamic key parts, with their type and value
 */
declare function decodeMappingKey(keyHash: string, keyNameOrSchema: string | ERC725JSONSchema): DynamicKeyPart[];

/**
 * @file lib/encoder.ts
 * @author Robert McLeod <@robertdavid010>
 * @author Fabian Vogelsteller <fabian@lukso.network>
 * @author Hugo Masclet <@Hugoo>
 * @author Callum Grindle <@CallumGrindle>
 * @author Jean Cavallera <@CJ42>
 * @date 2023
 */

declare const encodeDataSourceWithHash: (verification: undefined | Verification, dataSource: string) => string;
declare const decodeDataSourceWithHash: (value: string) => URLDataWithHash;
declare function encodeValueType(type: ERC725JSONSchemaValueType | string, // for tuples and CompactBytesArray,
value: string | string[] | bigint | bigint[] | number | number[] | boolean | boolean[]): string;
declare function decodeValueType(type: ERC725JSONSchemaValueType | string, // for tuples and CompactBytesArray
data: string, consumed?: ConsumedPtr): any;
declare function encodeValueContent(valueContent: string, value: string | bigint | AssetURLEncode | URLDataToEncode | boolean | unknown): string;
declare function decodeValueContent(valueContent: string, value: string): string | URLDataWithHash | bigint | boolean | null;

/**
 * @file lib/getDataFromExternalSources.ts
 * @author Hugo Masclet <@Hugoo>
 * @author Callum Grindle <@CallumGrindle>
 * @author Reto Ryter <@rryter>
 * @date 2021
 */

declare const getDataFromExternalSources: (schemas: ERC725JSONSchema[], dataFromChain: DecodeDataOutput[], ipfsGateway: string | ERC725Options) => Promise<GetDataExternalSourcesOutput[]>;

declare function encodePermissions(permissions: Permissions): Hex;
declare function decodePermissions(permissionHex: Hex): {
    CHANGEOWNER: boolean;
    ADDCONTROLLER: boolean;
    EDITPERMISSIONS: boolean;
    ADDEXTENSIONS: boolean;
    CHANGEEXTENSIONS: boolean;
    ADDUNIVERSALRECEIVERDELEGATE: boolean;
    CHANGEUNIVERSALRECEIVERDELEGATE: boolean;
    REENTRANCY: boolean;
    SUPER_TRANSFERVALUE: boolean;
    TRANSFERVALUE: boolean;
    SUPER_CALL: boolean;
    CALL: boolean;
    SUPER_STATICCALL: boolean;
    STATICCALL: boolean;
    SUPER_DELEGATECALL: boolean;
    DELEGATECALL: boolean;
    DEPLOY: boolean;
    SUPER_SETDATA: boolean;
    SETDATA: boolean;
    ENCRYPT: boolean;
    DECRYPT: boolean;
    SIGN: boolean;
    EXECUTE_RELAY_CALL: boolean;
    ERC4337_PERMISSION: boolean;
    ALL_PERMISSIONS: boolean;
};
/**
 * @notice Map a permission to its corresponding bytes32 representation.
 * @param permission The permission string to be mapped.
 * @return The bytes32 representation of the permission.
 * @dev Return null if the input is not a known permission name or a valid 32-byte hex string.
 */
declare function mapPermission(permission: string): string | null;
/**
 * @notice Check if the required permissions are included in the granted permissions.
 * @param requiredPermissions An array of required permissions or a single required permission.
 * @param grantedPermissions The granted permissions as a 32-byte hex string.
 * @return A boolean value indicating whether the required permissions are included in the granted permissions.
 * @dev Throws an error if the grantedPermissions input is not a valid 32-byte hex string.
 */
declare const checkPermissions: (requiredPermissions: string[] | string, _grantedPermissions: Hex) => boolean;

declare function getSchema(keyOrKeys: string | string[], providedSchemas?: ERC725JSONSchema[]): ERC725JSONSchema | null | Record<string, ERC725JSONSchema | null>;

declare function supportsInterface(interfaceIdOrName: string, options: {
    address: string;
    rpcUrl: string;
    gas?: number;
    provider?: any;
}): Promise<boolean>;
/**
 * This package is currently in early stages of development, <br/>use only for testing or experimentation purposes.<br/>
 *
 * @typeParam Schema
 *
 */
declare class ERC725 {
    options: ERC725Options;
    /**
     * Creates an instance of ERC725.
     * @param {ERC725JSONSchema[]} schema More information available here: [LSP-2-ERC725YJSONSchema](https://github.com/lukso-network/LIPs/blob/master/LSPs/LSP-2-ERC725YJSONSchema.md)
     * @param {string} address Address of the ERC725 contract you want to interact with
     * @param {any} provider
     * @param {ERC725Config} config Configuration object.
     *
     */
    constructor(schemas: ERC725JSONSchema[], address?: any, provider?: any, config?: ERC725Config);
    /**
     * To prevent weird behavior from the lib, we must make sure all the schemas are correct before loading them.
     *
     * @param schemas
     * @returns
     */
    private validateSchemas;
    private getAddressAndProvider;
    /**
     * Gets **decoded data** for one, many or all keys of the specified `ERC725` smart-contract.
     * When omitting the `keyOrKeys` parameter, it will get all the keys (as per {@link ERC725JSONSchema | ERC725JSONSchema} definition).
     *
     * Data returned by this function does not contain external data of [`JSONURL`](https://github.com/lukso-network/LIPs/blob/master/LSPs/LSP-2-ERC725YJSONSchema.md#jsonurl)
     * or [`ASSETURL`](https://github.com/lukso-network/LIPs/blob/master/LSPs/LSP-2-ERC725YJSONSchema.md#asseturl) schema elements.
     *
     * If you would like to receive everything in one go, you can use {@link ERC725.fetchData | `fetchData`} for that.
     *
     * @param {*} keyOrKeys The name (or the encoded name as the schema ‘key’) of the schema element in the class instance’s schema.
     *
     * @returns If the input is an array: an object with schema element key names as properties, with corresponding **decoded** data as values. If the input is a string, it directly returns the **decoded** data.
     */
    getData(keyOrKeys?: Array<string | GetDataDynamicKey>): Promise<DecodeDataOutput[]>;
    getData(keyOrKeys?: string | GetDataDynamicKey): Promise<DecodeDataOutput>;
    /**
     * Since {@link ERC725.getData | `getData`} exclusively returns data that is stored on the blockchain, `fetchData` comes in handy.
     * Additionally to the data from the blockchain, `fetchData` also returns data from IPFS or HTTP(s) endpoints
     * stored as [`JSONURL`](https://github.com/lukso-network/LIPs/blob/master/LSPs/LSP-2-ERC725YJSONSchema.md#jsonurl) or [`ASSETURL`](https://github.com/lukso-network/LIPs/blob/master/LSPs/LSP-2-ERC725YJSONSchema.md#asseturl).
     *
     * To ensure **data authenticity** `fetchData` compares the `hash` of the fetched JSON with the `hash` stored on the blockchain.
     *
     * @param {*} keyOrKeys The name (or the encoded name as the schema ‘key’) of the schema element in the class instance’s schema.
     *
     * @returns Returns the fetched and decoded value depending ‘valueContent’ for the schema element, otherwise works like getData.
     */
    fetchData(keyOrKeys?: Array<string | GetDataDynamicKey>): Promise<FetchDataOutput[]>;
    fetchData(keyOrKeys?: string | GetDataDynamicKey): Promise<FetchDataOutput>;
    /**
     * Parses a hashed key or a list of hashed keys and will attempt to return its corresponding LSP-2 ERC725YJSONSchema object.
     * The function will look for a corresponding key within the schemas:
     *  - in `./schemas` folder
     *  - provided at initialisation
     *  - provided in the function call
     *
     * @param keyOrKeys The hashed key or array of keys for which you want to find the corresponding LSP-2 ERC725YJSONSchema.
     * @param providedSchemas If you provide your own ERC725JSONSchemas, the parser will also try to find a key match against these schemas.
     */
    getSchema(keyOrKeys: string[], providedSchemas?: ERC725JSONSchema[]): Record<string, ERC725JSONSchema | null>;
    getSchema(keyOrKeys: string, providedSchemas?: ERC725JSONSchema[]): ERC725JSONSchema | null;
    /**
     * To be able to store your data on the blockchain, you need to encode it according to your {@link ERC725JSONSchema}.
     *
     * @param {{ [key: string]: any }} data An object with one or many properties, containing the data that needs to be encoded.
     * @param schemas Additionnal ERC725JSONSchemas which will be concatenated with the schemas provided on init.
     *
     * @returns An object with hashed keys and encoded values.
     *
     * When encoding JSON it is possible to pass in the JSON object and the URL where it is available publicly.
     * The JSON will be hashed with `keccak256`.
     */
    encodeData(data: EncodeDataInput[] | EncodeDataInput, schemas?: ERC725JSONSchema[]): EncodeDataReturn;
    /**
     * To be able to store your data on the blockchain, you need to encode it according to your {@link ERC725JSONSchema}.
     *
     * @param {{ [key: string]: any }} data An object with one or many properties, containing the data that needs to be encoded.
     * @param schemas ERC725JSONSchemas which will be used to encode the keys.
     *
     * @returns An object with hashed keys and encoded values.
     *
     * When encoding JSON it is possible to pass in the JSON object and the URL where it is available publicly.
     * The JSON will be hashed with `keccak256`.
     */
    static encodeData(data: EncodeDataInput[] | EncodeDataInput, schemas: ERC725JSONSchema[]): EncodeDataReturn;
    /**
     * In case you are reading the key-value store from an ERC725 smart-contract key-value store
     * without `@erc725/erc725.js` you can use `decodeData` to do the decoding for you.
     *
     * It is more convenient to use {@link ERC725.fetchData | `fetchData`}.
     * It does the `decoding` and `fetching` of external references for you automatically.
     *
     * @param {{ [key: string]: any }} data An object with one or many properties.
     * @param schemas ERC725JSONSchemas which will be used to encode the keys.
     *
     * @returns Returns decoded data as defined and expected in the schema:
     */
    decodeData(data: DecodeDataInput[], schemas?: ERC725JSONSchema[], throwExceptions?: boolean): {
        [key: string]: any;
    };
    /**
     * In case you are reading the key-value store from an ERC725 smart-contract key-value store
     * without `@erc725/erc725.js` you can use `decodeData` to do the decoding for you.
     *
     * It is more convenient to use {@link ERC725.fetchData | `fetchData`}.
     * It does the `decoding` and `fetching` of external references for you automatically.
     *
     * @param {{ [key: string]: any }} data An object with one or many properties.
     * @param schemas ERC725JSONSchemas which will be used to encode the keys.
     *
     * @returns Returns decoded data as defined and expected in the schema:
     */
    static decodeData(data: DecodeDataInput[], schemas: ERC725JSONSchema[], throwExceptions?: boolean): {
        [key: string]: any;
    };
    /**
     * An added utility method which simply returns the owner of the contract.
     * Not directly related to ERC725 specifications.
     *
     * @param {string} [address]
     * @returns The address of the contract owner as stored in the contract.
     *
     *    This method is not yet supported when using the `graph` provider type.
     *
     * ```javascript title="Example"
     * await myERC725.getOwner();
     * // '0x94933413384997F9402cc07a650e8A34d60F437A'
     *
     * await myERC725.getOwner("0x3000783905Cc7170cCCe49a4112Deda952DDBe24");
     * // '0x7f1b797b2Ba023Da2482654b50724e92EB5a7091'
     * ```
     */
    getOwner(_address?: string): Promise<any>;
    /**
     * A helper function which checks if a signature is valid according to the EIP-1271 standard.
     *
     * @param messageOrHash if it is a 66 chars string with 0x prefix, it will be considered as a hash (keccak256). If not, the message will be wrapped as follows: "\x19Ethereum Signed Message:\n" + message.length + message and hashed.
     * @param signature
     * @returns true if isValidSignature call on the contract returns the magic value. false otherwise
     */
    isValidSignature(messageOrHash: Hex | string, signature: Hex): Promise<boolean>;
    /**
     * Hashes a key name for use on an ERC725Y contract according to LSP2 ERC725Y JSONSchema standard.
     *
     * @param {string} keyName The key name you want to encode.
     * @param {DynamicKeyParts} dynamicKeyParts String or Array of String values used to construct the key.
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-2-ERC725YJSONSchema.md ERC725YJsonSchema standard.
     * @returns {string} The keccak256 hash of the provided key name. This is the key that must be retrievable from the ERC725Y contract via ERC725Y.getData(bytes32 key).
     */
    static encodeKeyName(keyName: string, dynamicKeyParts?: DynamicKeyParts): string;
    /**
     * Hashes a key name for use on an ERC725Y contract according to LSP2 ERC725Y JSONSchema standard.
     *
     * @param {string} keyName The key name you want to encode.
     * @param {DynamicKeyParts} dynamicKeyParts String or Array of String values used to construct the key.
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-2-ERC725YJSONSchema.md ERC725YJsonSchema standard.
     * @returns {string} The keccak256 hash of the provided key name. This is the key that must be retrievable from the ERC725Y contract via ERC725Y.getData(bytes32 key).
     */
    encodeKeyName(keyName: string, dynamicKeyParts?: DynamicKeyParts): string;
    /**
     * Decodes a hashed key used on an ERC725Y contract according to LSP2 ERC725Y JSONSchema standard.
     *
     * @param {string} keyHash Key hash that needs to be decoded.
     * @param {string | ERC725JSONSchema} keyNameOrSchema Key name following schema specifications or ERC725Y JSON Schema to follow in order to decode the key.
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-2-ERC725YJSONSchema.md ERC725YJsonSchema standard.
     * @returns {DynamicKeyPart[]} The Array with all the key decoded dynamic parameters. Each object have an attribute type and value.
     */
    static decodeMappingKey(keyHash: string, keyNameOrSchema: string | ERC725JSONSchema): DynamicKeyPart[];
    /**
     * Decodes a hashed key used on an ERC725Y contract according to LSP2 ERC725Y JSONSchema standard.
     *
     * @param {string} keyHash Key hash that needs to be decoded.
     * @param {string | ERC725JSONSchema} keyNameOrSchema Key name following schema specifications or ERC725Y JSON Schema to follow in order to decode the key.
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-2-ERC725YJSONSchema.md ERC725YJsonSchema standard.
     * @returns {DynamicKeyPart[]} The Array with all the key decoded dynamic parameters. Each object have an attribute type and value.
     */
    decodeMappingKey(keyHash: string, keyNameOrSchema: string | ERC725JSONSchema): DynamicKeyPart[];
    /**
     * Check if the ERC725 object supports
     * a certain interface.
     *
     * @param interfaceIdOrName Interface ID or supported interface name.
     * @returns {Promise<boolean>} if interface is supported.
     */
    supportsInterface(interfaceIdOrName: string): Promise<boolean>;
    /**
     * Check if a smart contract address
     * supports a certain interface.
     *
     * @param {string} interfaceIdOrName Interface ID or supported interface name.
     * @param options Object of address, RPC URL and optional gas.
     * @returns {Promise<boolean>} if interface is supported.
     */
    static supportsInterface(interfaceIdOrName: string, options: {
        address: string;
        rpcUrl: string;
        gas?: number;
    }): Promise<boolean>;
    /**
     * Encode permissions into a hexadecimal string as defined by the LSP6 KeyManager Standard.
     *
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-6-KeyManager.md LSP6 KeyManager Standard.
     * @param permissions The permissions you want to specify to be included or excluded. Any ommitted permissions will default to false.
     * @returns {*} The permissions encoded as a hexadecimal string as defined by the LSP6 Standard.
     */
    static encodePermissions(permissions: Permissions): string;
    /**
     * Encode permissions into a hexadecimal string as defined by the LSP6 KeyManager Standard.
     *
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-6-KeyManager.md LSP6 KeyManager Standard.
     * @param permissions The permissions you want to specify to be included or excluded. Any ommitted permissions will default to false.
     * @returns {*} The permissions encoded as a hexadecimal string as defined by the LSP6 Standard.
     */
    encodePermissions(permissions: Permissions): Hex;
    /**
     * Decodes permissions from hexadecimal as defined by the LSP6 KeyManager Standard.
     *
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-6-KeyManager.md LSP6 KeyManager Standard.
     * @param permissionHex The permission hexadecimal value to be decoded.
     * @returns Object specifying whether default LSP6 permissions are included in provided hexademical string.
     */
    static decodePermissions(permissionHex: Hex): {
        CHANGEOWNER: boolean;
        ADDCONTROLLER: boolean;
        EDITPERMISSIONS: boolean;
        ADDEXTENSIONS: boolean;
        CHANGEEXTENSIONS: boolean;
        ADDUNIVERSALRECEIVERDELEGATE: boolean;
        CHANGEUNIVERSALRECEIVERDELEGATE: boolean;
        REENTRANCY: boolean;
        SUPER_TRANSFERVALUE: boolean;
        TRANSFERVALUE: boolean;
        SUPER_CALL: boolean;
        CALL: boolean;
        SUPER_STATICCALL: boolean;
        STATICCALL: boolean;
        SUPER_DELEGATECALL: boolean;
        DELEGATECALL: boolean;
        DEPLOY: boolean;
        SUPER_SETDATA: boolean;
        SETDATA: boolean;
        ENCRYPT: boolean;
        DECRYPT: boolean;
        SIGN: boolean;
        EXECUTE_RELAY_CALL: boolean;
        ERC4337_PERMISSION: boolean;
        ALL_PERMISSIONS: boolean;
    };
    /**
     * Decodes permissions from hexadecimal as defined by the LSP6 KeyManager Standard.
     *
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-6-KeyManager.md LSP6 KeyManager Standard.
     * @param permissionHex The permission hexadecimal value to be decoded.
     * @returns Object specifying whether default LSP6 permissions are included in provided hexademical string.
     */
    decodePermissions(permissionHex: Hex): {
        CHANGEOWNER: boolean;
        ADDCONTROLLER: boolean;
        EDITPERMISSIONS: boolean;
        ADDEXTENSIONS: boolean;
        CHANGEEXTENSIONS: boolean;
        ADDUNIVERSALRECEIVERDELEGATE: boolean;
        CHANGEUNIVERSALRECEIVERDELEGATE: boolean;
        REENTRANCY: boolean;
        SUPER_TRANSFERVALUE: boolean;
        TRANSFERVALUE: boolean;
        SUPER_CALL: boolean;
        CALL: boolean;
        SUPER_STATICCALL: boolean;
        STATICCALL: boolean;
        SUPER_DELEGATECALL: boolean;
        DELEGATECALL: boolean;
        DEPLOY: boolean;
        SUPER_SETDATA: boolean;
        SETDATA: boolean;
        ENCRYPT: boolean;
        DECRYPT: boolean;
        SIGN: boolean;
        EXECUTE_RELAY_CALL: boolean;
        ERC4337_PERMISSION: boolean;
        ALL_PERMISSIONS: boolean;
    };
    /**
     * Check if the required permissions are included in the granted permissions as defined by the LSP6 KeyManager Standard.
     *
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-6-KeyManager.md LSP6 KeyManager Standard.
     * @param requiredPermissions An array of required permissions or a single required permission.
     * @param grantedPermissions The granted permissions as a 32-byte hex string.
     * @return A boolean value indicating whether the required permissions are included in the granted permissions.
     */
    static checkPermissions(requiredPermissions: string[] | string, grantedPermissions: Hex): boolean;
    /**
     * Check if the required permissions are included in the granted permissions as defined by the LSP6 KeyManager Standard.
     *
     * @link https://github.com/lukso-network/LIPs/blob/main/LSPs/LSP-6-KeyManager.md LSP6 KeyManager Standard.
     * @param requiredPermissions An array of required permissions or a single required permission.
     * @param grantedPermissions The granted permissions as a 32-byte hex string.
     * @return A boolean value indicating whether the required permissions are included in the granted permissions.
     */
    checkPermissions(requiredPermissions: string[] | string, grantedPermissions: Hex): boolean;
    static mapPermission(permission: string): string | null;
    mapPermission(permission: string): string | null;
    /**
     * @param type The valueType to encode the value as
     * @param value The value to encode
     * @returns The encoded value
     */
    static encodeValueType(type: string, value: string | string[] | bigint | bigint[] | boolean | boolean[]): string;
    encodeValueType(type: string, value: string | string[] | bigint | bigint[] | boolean | boolean[]): string;
    /**
     * @param type The valueType to decode the value as
     * @param data The data to decode
     * @returns The decoded value
     */
    static decodeValueType(type: string, data: string): any;
    decodeValueType(type: string, data: string): any;
    static encodeValueContent(valueContent: string, value: string | number | bigint | AssetURLEncode | URLDataToEncode | boolean): string;
    encodeValueContent(valueContent: string, value: string | number | bigint | AssetURLEncode | URLDataToEncode | boolean): string;
    static decodeValueContent(valueContent: string, value: string): string | URLDataWithHash | bigint | boolean | null;
    decodeValueContent(valueContent: string, value: string): string | URLDataWithHash | bigint | boolean | null;
    encodeDataSourceWithHash(verification: undefined | Verification, dataSource: string): string;
    static encodeDataSourceWithHash(verification: undefined | Verification, dataSource: string): string;
    decodeDataSourceWithHash(value: string): URLDataWithHash;
    static decodeDataSourceWithHash(value: string): URLDataWithHash;
    static getVerificationMethod(nameOrSig: string): {
        method: (data: string | object | Uint8Array | null) => string;
        name: SUPPORTED_VERIFICATION_METHOD_STRINGS;
        sig: SUPPORTED_VERIFICATION_METHODS;
    } | undefined;
    getVerificationMethod(nameOrSig: string): {
        method: (data: string | object | Uint8Array | null) => string;
        name: SUPPORTED_VERIFICATION_METHOD_STRINGS;
        sig: SUPPORTED_VERIFICATION_METHODS;
    } | undefined;
    static isDataAuthentic(data: string | Uint8Array, verificationOptions: Verification): boolean;
    isDataAuthentic(data: string | Uint8Array, verificationOptions: Verification): boolean;
}

export { type DecodeDataInput, type DecodeDataOutput, ERC725, type ERC725Config, ERC725JSONSchema, ERC725JSONSchemaValueType, type ERC725Options, type EncodeDataReturn, type FetchDataOutput, type GetDataDynamicKey, type GetDataExternalSourcesOutput, type GetDataInput, type KeyValuePair, type Permissions, ProviderTypes, type SUPPORTED_VERIFICATION_METHODS, SUPPORTED_VERIFICATION_METHOD_STRINGS, type URLDataWithHash, type Verification, checkPermissions, decodeData, decodeDataSourceWithHash, decodeMappingKey, decodePermissions, decodeValueContent, decodeValueType, ERC725 as default, encodeArrayKey, encodeData, encodeDataSourceWithHash, encodeKeyName, encodePermissions, encodeValueContent, encodeValueType, getDataFromExternalSources, getSchema, getVerificationMethod, isDataAuthentic, isDynamicKeyName, mapPermission, patchIPFSUrlsIfApplicable, supportsInterface };
